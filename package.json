{
  "name": "playhouse",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "postinstall": "husky install"
  },
  "devDependencies": {
    "husky": "^7.0.1",
    "prettier": "^2.2.1",
    "pretty-quick": "^3.1.0"
  }
}
