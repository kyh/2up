{
  "name": "coinop",
  "scripts": {
    "dev:web": "npm run dev --prefix=web",
    "dev:voice": "npm run dev --prefix=voice",
    "postinstall": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "prettier": "^2.5.1",
    "pretty-quick": "^3.1.2"
  }
}
